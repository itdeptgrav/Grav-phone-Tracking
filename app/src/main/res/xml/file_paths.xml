<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exposes app-private recordings via FileProvider so they can be shared
         out (Drive, email, WhatsApp, etc.) without world-readable storage. -->
    <files-path name="recordings" path="recordings/" />
</paths>
